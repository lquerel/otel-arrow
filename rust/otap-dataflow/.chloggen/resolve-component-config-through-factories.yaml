change_type: enhancement
component: engine
note: "Resolve component configuration through factories before runtime admission and expose only safe effective configuration snapshots."
issues: [3848]
subtext: |
  Factory registrations now declare typed resolution and an explicit snapshot policy. Secrets are redacted and unsafe or free-form component configuration is omitted from Admin and OpAMP output.
