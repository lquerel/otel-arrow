// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// @generated by OTel Weaver. DO NOT EDIT.

//! Generated client for the `processor.durable_buffer.items` metric set.

use crate::entities;
use otap_df_telemetry::descriptor::{Instrument, MetricValueType, MetricsDescriptor, MetricsField};
use otap_df_telemetry::metrics::{MetricSet, MetricSetHandler, MetricValue};
use otap_df_telemetry::registry::TelemetryRegistryHandle;

/// Canonical metric-set name and instrumentation scope.
pub const METRIC_SET: &str = "processor.durable_buffer.items";

/// Workspace package containing the current hand-written declaration.
pub const SOURCE_PACKAGE: &str = "otap-df-core-nodes";

/// Conditional availability inherited by the complete metric set.
pub const AVAILABILITY: Option<&str> = None;

/// Marker implemented only by entity types allowed by this metric set's
/// semantic-convention `entity_associations` expression.
pub trait AssociatedEntity: entities::SemanticEntity {}

impl AssociatedEntity for entities::NodeAttributeSet {}

impl AssociatedEntity for entities::NodeWithCustomAttributeSet {}

/// Strongly typed values for the `processor.durable_buffer.items` metric set.
#[derive(Debug, Default, Clone)]
#[repr(C, align(64))]
pub struct DurableBufferItemMetrics {
    /// Number of items ingested to durable storage.
    pub consumed: otap_df_telemetry::instrument::Counter<u64>,
    /// Number of items sent downstream.
    pub produced: otap_df_telemetry::instrument::Counter<u64>,
    /// Current number of items queued in durable storage.
    pub queued: otap_df_telemetry::instrument::Gauge<u64>,
    /// Number of items in permanently rejected bundles.
    pub rejected: otap_df_telemetry::instrument::Counter<u64>,
    /// Number of items requeued for retry after a NACK.
    pub requeued: otap_df_telemetry::instrument::Counter<u64>,
}

/// Per-field conditional availability in descriptor order.
pub const DURABLE_BUFFER_ITEM_METRICS_METRIC_AVAILABILITY: &[Option<&str>] = &[
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
];

static DURABLE_BUFFER_ITEM_METRICS_DESCRIPTOR: MetricsDescriptor = MetricsDescriptor {
    name: METRIC_SET,
    metrics: &[
        MetricsField {
            name: "consumed",
            unit: "{item}",
            brief: "Number of items ingested to durable storage.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "produced",
            unit: "{item}",
            brief: "Number of items sent downstream.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "queued",
            unit: "{item}",
            brief: "Current number of items queued in durable storage.",
            instrument: Instrument::Gauge,
            temporality: None,
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "rejected",
            unit: "{item}",
            brief: "Number of items in permanently rejected bundles.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "requeued",
            unit: "{item}",
            brief: "Number of items requeued for retry after a NACK.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
    ],
};

impl MetricSetHandler for DurableBufferItemMetrics {
    #[inline]
    fn descriptor(&self) -> &'static MetricsDescriptor {
        &DURABLE_BUFFER_ITEM_METRICS_DESCRIPTOR
    }

    #[inline]
    fn snapshot_values(&self) -> Vec<MetricValue> {
        vec![
            MetricValue::from(self.consumed.get()),
            MetricValue::from(self.produced.get()),
            MetricValue::from(self.queued.get()),
            MetricValue::from(self.rejected.get()),
            MetricValue::from(self.requeued.get()),
        ]
    }

    #[inline]
    fn clear_values(&mut self) {
        self.consumed.reset();
        self.produced.reset();
        self.rejected.reset();
        self.requeued.reset();
    }

    #[inline]
    fn needs_flush(&self) -> bool {
        if !MetricValue::from(self.consumed.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.produced.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.rejected.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.requeued.get()).is_zero() {
            return true;
        }
        true
    }
}

impl DurableBufferItemMetrics {
    /// Registers this metric set against a semantically compatible entity.
    #[must_use]
    pub fn register<E>(registry: &TelemetryRegistryHandle, entity: E) -> MetricSet<Self>
    where
        E: AssociatedEntity,
    {
        registry.register_metric_set(entity)
    }
}
