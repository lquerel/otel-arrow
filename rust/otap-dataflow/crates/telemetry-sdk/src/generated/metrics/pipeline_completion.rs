// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// @generated by OTel Weaver. DO NOT EDIT.

//! Generated client for the `pipeline.completion` metric set.

use crate::entities;
use otap_df_telemetry::descriptor::{Instrument, MetricValueType, MetricsDescriptor, MetricsField};
use otap_df_telemetry::metrics::{MetricSet, MetricSetHandler, MetricValue};
use otap_df_telemetry::registry::TelemetryRegistryHandle;

/// Canonical metric-set name and instrumentation scope.
pub const METRIC_SET: &str = "pipeline.completion";

/// Workspace package containing the current hand-written declaration.
pub const SOURCE_PACKAGE: &str = "otap-df-engine";

/// Conditional availability inherited by the complete metric set.
pub const AVAILABILITY: Option<&str> = None;

/// Marker implemented only by entity types allowed by this metric set's
/// semantic-convention `entity_associations` expression.
pub trait AssociatedEntity: entities::SemanticEntity {}

impl AssociatedEntity for entities::PipelineAttributeSet {}

/// Strongly typed values for the `pipeline.completion` metric set.
#[derive(Debug, Default, Clone)]
#[repr(C, align(64))]
pub struct PipelineCompletionMetrics {
    /// Count of Ack completions routed toward an interested upstream node. This increments once the dispatcher finds the closest interested frame and attempts delivery, even if the node-control channel is temporarily full and the send must be retried later. Level: `normal`.
    pub ack_attempted: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of Ack control messages actually delivered to upstream nodes. This increments only when the node-control send succeeds, either immediately or later via the completion dispatcher's pending-send retry loop. Level: `normal`.
    pub ack_delivered: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of Ack completions dropped because no upstream frame was interested. This distinguishes "successful unwind with no listener" from send failure or dispatcher backlog, which is useful when validating routing expectations and subscription placement. Level: `normal`.
    pub ack_dropped_no_interest: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of `DeliverAck` messages received by the completion dispatcher. This shows inbound successful completion pressure on the shared completion path and helps explain downstream-to-upstream unwind load. Level: `normal`.
    pub deliver_ack_received: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of `DeliverNack` messages received by the completion dispatcher. This is the failure-side counterpart to received acks and is especially useful when investigating retry storms or elevated downstream refusal rates. Level: `normal`.
    pub deliver_nack_received: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of Nack completions routed toward an interested upstream node. This is the failure-side counterpart to attempted Acks and captures how many downstream refusals found an upstream subscriber and entered the delivery path. Level: `normal`.
    pub nack_attempted: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of Nack control messages actually delivered to upstream nodes. This is the delivery-side counterpart to attempted Nacks and helps distinguish routing intent from successful node-control delivery under backpressure. Level: `normal`.
    pub nack_delivered: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of Nack completions dropped because no upstream frame was interested. This is particularly important for liveness and correctness analysis: it means failure information reached the dispatcher but no upstream frame subscribed to receive it. Level: `normal`.
    pub nack_dropped_no_interest: otap_df_telemetry::instrument::Counter<u64>,
    /// Number of completion deliveries currently buffered for later retry. A sustained backlog here means the completion dispatcher is making progress by deferring sends to full node-control inboxes instead of blocking, but it also indicates unwind traffic is under pressure. Level: `basic`.
    pub pending_sends_buffered: otap_df_telemetry::instrument::Gauge<u64>,
    /// Distribution of unwind stack depth consumed per completion outcome. Larger values indicate completions are traversing more frames before finding an interested upstream node or concluding that none exists, which helps explain completion-path cost and routing behavior. Level: `detailed`.
    pub unwind_depth: otap_df_telemetry::instrument::Mmsc,
}

/// Per-field conditional availability in descriptor order.
pub const PIPELINE_COMPLETION_METRICS_METRIC_AVAILABILITY: &[Option<&str>] = &[
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
];

static PIPELINE_COMPLETION_METRICS_DESCRIPTOR: MetricsDescriptor = MetricsDescriptor {
    name: METRIC_SET,
    metrics: &[
        MetricsField {
            name: "ack.attempted",
            unit: "{message}",
            brief: "Count of Ack completions routed toward an interested upstream node. This increments once the dispatcher finds the closest interested frame and attempts delivery, even if the node-control channel is temporarily full and the send must be retried later. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "ack.delivered",
            unit: "{message}",
            brief: "Count of Ack control messages actually delivered to upstream nodes. This increments only when the node-control send succeeds, either immediately or later via the completion dispatcher's pending-send retry loop. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "ack.dropped_no_interest",
            unit: "{message}",
            brief: "Count of Ack completions dropped because no upstream frame was interested. This distinguishes \"successful unwind with no listener\" from send failure or dispatcher backlog, which is useful when validating routing expectations and subscription placement. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "deliver_ack.received",
            unit: "{message}",
            brief: "Count of `DeliverAck` messages received by the completion dispatcher. This shows inbound successful completion pressure on the shared completion path and helps explain downstream-to-upstream unwind load. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "deliver_nack.received",
            unit: "{message}",
            brief: "Count of `DeliverNack` messages received by the completion dispatcher. This is the failure-side counterpart to received acks and is especially useful when investigating retry storms or elevated downstream refusal rates. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "nack.attempted",
            unit: "{message}",
            brief: "Count of Nack completions routed toward an interested upstream node. This is the failure-side counterpart to attempted Acks and captures how many downstream refusals found an upstream subscriber and entered the delivery path. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "nack.delivered",
            unit: "{message}",
            brief: "Count of Nack control messages actually delivered to upstream nodes. This is the delivery-side counterpart to attempted Nacks and helps distinguish routing intent from successful node-control delivery under backpressure. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "nack.dropped_no_interest",
            unit: "{message}",
            brief: "Count of Nack completions dropped because no upstream frame was interested. This is particularly important for liveness and correctness analysis: it means failure information reached the dispatcher but no upstream frame subscribed to receive it. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "pending_sends.buffered",
            unit: "{message}",
            brief: "Number of completion deliveries currently buffered for later retry. A sustained backlog here means the completion dispatcher is making progress by deferring sends to full node-control inboxes instead of blocking, but it also indicates unwind traffic is under pressure. Level: `basic`.",
            instrument: Instrument::Gauge,
            temporality: None,
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "unwind.depth",
            unit: "{frame}",
            brief: "Distribution of unwind stack depth consumed per completion outcome. Larger values indicate completions are traversing more frames before finding an interested upstream node or concluding that none exists, which helps explain completion-path cost and routing behavior. Level: `detailed`.",
            instrument: Instrument::Mmsc,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::F64,
        },
    ],
};

impl MetricSetHandler for PipelineCompletionMetrics {
    #[inline]
    fn descriptor(&self) -> &'static MetricsDescriptor {
        &PIPELINE_COMPLETION_METRICS_DESCRIPTOR
    }

    #[inline]
    fn snapshot_values(&self) -> Vec<MetricValue> {
        vec![
            MetricValue::from(self.ack_attempted.get()),
            MetricValue::from(self.ack_delivered.get()),
            MetricValue::from(self.ack_dropped_no_interest.get()),
            MetricValue::from(self.deliver_ack_received.get()),
            MetricValue::from(self.deliver_nack_received.get()),
            MetricValue::from(self.nack_attempted.get()),
            MetricValue::from(self.nack_delivered.get()),
            MetricValue::from(self.nack_dropped_no_interest.get()),
            MetricValue::from(self.pending_sends_buffered.get()),
            MetricValue::from(self.unwind_depth.get()),
        ]
    }

    #[inline]
    fn clear_values(&mut self) {
        self.ack_attempted.reset();
        self.ack_delivered.reset();
        self.ack_dropped_no_interest.reset();
        self.deliver_ack_received.reset();
        self.deliver_nack_received.reset();
        self.nack_attempted.reset();
        self.nack_delivered.reset();
        self.nack_dropped_no_interest.reset();
        self.unwind_depth.reset();
    }

    #[inline]
    fn needs_flush(&self) -> bool {
        if !MetricValue::from(self.ack_attempted.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.ack_delivered.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.ack_dropped_no_interest.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.deliver_ack_received.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.deliver_nack_received.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.nack_attempted.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.nack_delivered.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.nack_dropped_no_interest.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.unwind_depth.get()).is_zero() {
            return true;
        }
        true
    }
}

impl PipelineCompletionMetrics {
    /// Registers this metric set against a semantically compatible entity.
    #[must_use]
    pub fn register<E>(registry: &TelemetryRegistryHandle, entity: E) -> MetricSet<Self>
    where
        E: AssociatedEntity,
    {
        registry.register_metric_set(entity)
    }
}
