// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// @generated by OTel Weaver. DO NOT EDIT.

//! Generated client for the `otap.exporter.parquet` metric set.

use crate::entities;
use otap_df_telemetry::descriptor::{Instrument, MetricValueType, MetricsDescriptor, MetricsField};
use otap_df_telemetry::metrics::{MetricSet, MetricSetHandler, MetricValue};
use otap_df_telemetry::registry::TelemetryRegistryHandle;

/// Canonical metric-set name and instrumentation scope.
pub const METRIC_SET: &str = "otap.exporter.parquet";

/// Workspace package containing the current hand-written declaration.
pub const SOURCE_PACKAGE: &str = "otap-df-core-nodes";

/// Conditional availability inherited by the complete metric set.
pub const AVAILABILITY: Option<&str> = None;

/// Marker implemented only by entity types allowed by this metric set's
/// semantic-convention `entity_associations` expression.
pub trait AssociatedEntity: entities::SemanticEntity {}

impl AssociatedEntity for entities::NodeAttributeSet {}

impl AssociatedEntity for entities::NodeWithCustomAttributeSet {}

/// Strongly typed values for the `otap.exporter.parquet` metric set.
#[derive(Debug, Default, Clone)]
#[repr(C, align(64))]
pub struct ParquetExporterMetrics {
    /// Number of Parquet files successfully closed (flushed and visible to readers).
    pub files_closed: otap_df_telemetry::instrument::Counter<u64>,
    /// Number of Parquet files created (across all payload types and partitions).
    pub files_created: otap_df_telemetry::instrument::Counter<u64>,
    /// File close/flush attempts initiated by the exporter.
    pub flush_attempts: otap_df_telemetry::instrument::Counter<u64>,
    /// File close/flush attempts that failed after the lower-level retry policy was exhausted.
    pub flush_failures: otap_df_telemetry::instrument::Counter<u64>,
    /// Files scheduled for flush due to exceeding max age threshold.
    pub flush_scheduled_max_age: otap_df_telemetry::instrument::Counter<u64>,
    /// Files scheduled for flush due to reaching target rows per file.
    pub flush_scheduled_max_rows: otap_df_telemetry::instrument::Counter<u64>,
    /// File close/flush attempts that succeeded and made the file visible to readers.
    pub flush_successes: otap_df_telemetry::instrument::Counter<u64>,
    /// Total number of rows written into Parquet writers (appended, not necessarily flushed yet).
    pub rows_written: otap_df_telemetry::instrument::Counter<u64>,
}

/// Per-field conditional availability in descriptor order.
pub const PARQUET_EXPORTER_METRICS_METRIC_AVAILABILITY: &[Option<&str>] = &[
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
];

static PARQUET_EXPORTER_METRICS_DESCRIPTOR: MetricsDescriptor = MetricsDescriptor {
    name: METRIC_SET,
    metrics: &[
        MetricsField {
            name: "files.closed",
            unit: "{file}",
            brief: "Number of Parquet files successfully closed (flushed and visible to readers).",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "files.created",
            unit: "{file}",
            brief: "Number of Parquet files created (across all payload types and partitions).",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "flush.attempts",
            unit: "{file}",
            brief: "File close/flush attempts initiated by the exporter.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "flush.failures",
            unit: "{file}",
            brief: "File close/flush attempts that failed after the lower-level retry policy was exhausted.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "flush.scheduled.max.age",
            unit: "{file}",
            brief: "Files scheduled for flush due to exceeding max age threshold.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "flush.scheduled.max.rows",
            unit: "{file}",
            brief: "Files scheduled for flush due to reaching target rows per file.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "flush.successes",
            unit: "{file}",
            brief: "File close/flush attempts that succeeded and made the file visible to readers.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "rows.written",
            unit: "{row}",
            brief: "Total number of rows written into Parquet writers (appended, not necessarily flushed yet).",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
    ],
};

impl MetricSetHandler for ParquetExporterMetrics {
    #[inline]
    fn descriptor(&self) -> &'static MetricsDescriptor {
        &PARQUET_EXPORTER_METRICS_DESCRIPTOR
    }

    #[inline]
    fn snapshot_values(&self) -> Vec<MetricValue> {
        vec![
            MetricValue::from(self.files_closed.get()),
            MetricValue::from(self.files_created.get()),
            MetricValue::from(self.flush_attempts.get()),
            MetricValue::from(self.flush_failures.get()),
            MetricValue::from(self.flush_scheduled_max_age.get()),
            MetricValue::from(self.flush_scheduled_max_rows.get()),
            MetricValue::from(self.flush_successes.get()),
            MetricValue::from(self.rows_written.get()),
        ]
    }

    #[inline]
    fn clear_values(&mut self) {
        self.files_closed.reset();
        self.files_created.reset();
        self.flush_attempts.reset();
        self.flush_failures.reset();
        self.flush_scheduled_max_age.reset();
        self.flush_scheduled_max_rows.reset();
        self.flush_successes.reset();
        self.rows_written.reset();
    }

    #[inline]
    fn needs_flush(&self) -> bool {
        if !MetricValue::from(self.files_closed.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.files_created.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.flush_attempts.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.flush_failures.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.flush_scheduled_max_age.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.flush_scheduled_max_rows.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.flush_successes.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.rows_written.get()).is_zero() {
            return true;
        }
        false
    }
}

impl ParquetExporterMetrics {
    /// Registers this metric set against a semantically compatible entity.
    #[must_use]
    pub fn register<E>(registry: &TelemetryRegistryHandle, entity: E) -> MetricSet<Self>
    where
        E: AssociatedEntity,
    {
        registry.register_metric_set(entity)
    }
}
