// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// @generated by OTel Weaver. DO NOT EDIT.

//! Generated client for the `exporter.file` metric set.

use crate::entities;
use otap_df_telemetry::descriptor::{Instrument, MetricValueType, MetricsDescriptor, MetricsField};
use otap_df_telemetry::metrics::{MetricSet, MetricSetHandler, MetricValue};
use otap_df_telemetry::registry::TelemetryRegistryHandle;

/// Canonical metric-set name and instrumentation scope.
pub const METRIC_SET: &str = "exporter.file";

/// Workspace package containing the current hand-written declaration.
pub const SOURCE_PACKAGE: &str = "otap-df-core-nodes";

/// Conditional availability inherited by the complete metric set.
pub const AVAILABILITY: Option<&str> = None;

/// Marker implemented only by entity types allowed by this metric set's
/// semantic-convention `entity_associations` expression.
pub trait AssociatedEntity: entities::SemanticEntity {}

impl AssociatedEntity for entities::NodeAttributeSet {}

impl AssociatedEntity for entities::NodeWithCustomAttributeSet {}

/// Strongly typed values for the `exporter.file` metric set.
#[derive(Debug, Default, Clone)]
#[repr(C, align(64))]
pub struct FileFailureMetrics {
    /// Open, write, sync, or rollback failures.
    pub failures: otap_df_telemetry::instrument::Counter<u64>,
}

/// Per-field conditional availability in descriptor order.
pub const FILE_FAILURE_METRICS_METRIC_AVAILABILITY: &[Option<&str>] = &[AVAILABILITY];

static FILE_FAILURE_METRICS_DESCRIPTOR: MetricsDescriptor = MetricsDescriptor {
    name: METRIC_SET,
    metrics: &[MetricsField {
        name: "failures",
        unit: "{failure}",
        brief: "Open, write, sync, or rollback failures.",
        instrument: Instrument::Counter,
        temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
        value_type: MetricValueType::U64,
    }],
};

impl MetricSetHandler for FileFailureMetrics {
    #[inline]
    fn descriptor(&self) -> &'static MetricsDescriptor {
        &FILE_FAILURE_METRICS_DESCRIPTOR
    }

    #[inline]
    fn snapshot_values(&self) -> Vec<MetricValue> {
        vec![MetricValue::from(self.failures.get())]
    }

    #[inline]
    fn clear_values(&mut self) {
        self.failures.reset();
    }

    #[inline]
    fn needs_flush(&self) -> bool {
        if !MetricValue::from(self.failures.get()).is_zero() {
            return true;
        }
        false
    }
}

impl FileFailureMetrics {
    /// Registers this metric set against a semantically compatible entity.
    #[must_use]
    pub fn register<E>(registry: &TelemetryRegistryHandle, entity: E) -> MetricSet<Self>
    where
        E: AssociatedEntity,
    {
        registry.register_metric_set(entity)
    }
}

/// Strongly typed values for the `exporter.file` metric set.
#[derive(Debug, Default, Clone)]
#[repr(C, align(64))]
pub struct FileSignalMetrics {
    /// Successfully written frame bytes, including newline delimiters.
    pub bytes: otap_df_telemetry::instrument::Counter<u64>,
    /// Signal items in frames successfully written before ACK routing.
    pub items: otap_df_telemetry::instrument::Counter<u64>,
    /// Bytes removed by successful append-tail recovery.
    pub tail_recovered_bytes: otap_df_telemetry::instrument::Counter<u64>,
    /// Append-mode partial tails successfully repaired.
    pub tail_recoveries: otap_df_telemetry::instrument::Counter<u64>,
}

/// Per-field conditional availability in descriptor order.
pub const FILE_SIGNAL_METRICS_METRIC_AVAILABILITY: &[Option<&str>] =
    &[AVAILABILITY, AVAILABILITY, AVAILABILITY, AVAILABILITY];

static FILE_SIGNAL_METRICS_DESCRIPTOR: MetricsDescriptor = MetricsDescriptor {
    name: METRIC_SET,
    metrics: &[
        MetricsField {
            name: "bytes",
            unit: "By",
            brief: "Successfully written frame bytes, including newline delimiters.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "items",
            unit: "{item}",
            brief: "Signal items in frames successfully written before ACK routing.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "tail.recovered.bytes",
            unit: "By",
            brief: "Bytes removed by successful append-tail recovery.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "tail.recoveries",
            unit: "{recovery}",
            brief: "Append-mode partial tails successfully repaired.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
    ],
};

impl MetricSetHandler for FileSignalMetrics {
    #[inline]
    fn descriptor(&self) -> &'static MetricsDescriptor {
        &FILE_SIGNAL_METRICS_DESCRIPTOR
    }

    #[inline]
    fn snapshot_values(&self) -> Vec<MetricValue> {
        vec![
            MetricValue::from(self.bytes.get()),
            MetricValue::from(self.items.get()),
            MetricValue::from(self.tail_recovered_bytes.get()),
            MetricValue::from(self.tail_recoveries.get()),
        ]
    }

    #[inline]
    fn clear_values(&mut self) {
        self.bytes.reset();
        self.items.reset();
        self.tail_recovered_bytes.reset();
        self.tail_recoveries.reset();
    }

    #[inline]
    fn needs_flush(&self) -> bool {
        if !MetricValue::from(self.bytes.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.items.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.tail_recovered_bytes.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.tail_recoveries.get()).is_zero() {
            return true;
        }
        false
    }
}

impl FileSignalMetrics {
    /// Registers this metric set against a semantically compatible entity.
    #[must_use]
    pub fn register<E>(registry: &TelemetryRegistryHandle, entity: E) -> MetricSet<Self>
    where
        E: AssociatedEntity,
    {
        registry.register_metric_set(entity)
    }
}
