// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// @generated by OTel Weaver. DO NOT EDIT.

//! Generated client for the `pipeline.runtime_control` metric set.

use crate::entities;
use otap_df_telemetry::descriptor::{Instrument, MetricValueType, MetricsDescriptor, MetricsField};
use otap_df_telemetry::metrics::{MetricSet, MetricSetHandler, MetricValue};
use otap_df_telemetry::registry::TelemetryRegistryHandle;

/// Canonical metric-set name and instrumentation scope.
pub const METRIC_SET: &str = "pipeline.runtime_control";

/// Workspace package containing the current hand-written declaration.
pub const SOURCE_PACKAGE: &str = "otap-df-engine";

/// Conditional availability inherited by the complete metric set.
pub const AVAILABILITY: Option<&str> = None;

/// Marker implemented only by entity types allowed by this metric set's
/// semantic-convention `entity_associations` expression.
pub trait AssociatedEntity: entities::SemanticEntity {}

impl AssociatedEntity for entities::PipelineAttributeSet {}

/// Strongly typed values for the `pipeline.runtime_control` metric set.
#[derive(Debug, Default, Clone)]
#[repr(C, align(64))]
pub struct RuntimeControlMetrics {
    /// Count of telemetry timer cancel requests received by the runtime manager. This is mainly useful for checking that telemetry scheduling remains bounded and does not accumulate stale per-node timers. Level: `normal`.
    pub cancel_telemetry_timer_received: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of `CancelTimer` control messages received by the runtime manager. Comparing this with timer starts helps diagnose timer churn, leaks, or mismatches between node scheduling and node cleanup behavior. Level: `normal`.
    pub cancel_timer_received: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of `CollectTelemetry` control messages sent to nodes. This helps confirm telemetry timers are progressing under load and that observability work is not being starved by other runtime-control traffic. Level: `normal`.
    pub collect_telemetry_sent: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of `DelayData` requests received by the runtime manager. This measures how much work is being converted into deferred wakeups, a common cause of control-plane load for retry and batching patterns. Level: `normal`.
    pub delay_data_received: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of delayed-data items returned immediately because drain is active. This is important for shutdown liveness: it shows the runtime is flushing scheduled work back to origin nodes instead of leaving it stranded behind future wakeup times. Level: `normal`.
    pub delay_data_returned_during_drain: otap_df_telemetry::instrument::Counter<u64>,
    /// Number of delayed-data wakeups currently queued in the runtime manager. This is a key liveness signal for processors that depend on delayed retries or one-shot wakeups: rising backlog means more admitted work is waiting on the shared runtime control path. Level: `basic`.
    pub delayed_data_queued: otap_df_telemetry::instrument::Gauge<u64>,
    /// Count of delayed-data wakeups sent back to origin nodes. This is the completion-side counterpart to `delay_data.received` and is a direct indicator that deferred work is resuming rather than stalling. Level: `normal`.
    pub delayed_data_sent: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of downstream `Shutdown` fan-out operations to non-receivers. This marks the handoff from receiver draining to downstream convergence, which is useful when debugging where shutdown latency is spent. Level: `normal`.
    pub downstream_shutdown_sent: otap_df_telemetry::instrument::Counter<u64>,
    /// Whether the runtime is currently in graceful drain mode. This is the top-level liveness phase signal for shutdown analysis: `1` means ingress has been stopped and the runtime is trying to converge to quiescence, `0` means normal operation. Level: `basic`.
    pub drain_active: otap_df_telemetry::instrument::Gauge<u64>,
    /// Number of receivers that still need to report `ReceiverDrained`. This shows whether shutdown is still blocked in the receiver-first drain phase and helps identify a stuck or slow receiver delaying downstream shutdown. Level: `basic`.
    pub drain_pending_receivers: otap_df_telemetry::instrument::Gauge<u64>,
    /// Distribution of time spent waiting for receivers to finish draining. This isolates the receiver-gated phase of shutdown and is useful for determining whether slow graceful shutdown is ingress-bound or further downstream in the pipeline. Level: `detailed`.
    pub drain_receiver_phase_duration_ns: otap_df_telemetry::instrument::Mmsc,
    /// Distribution of total graceful shutdown duration for the runtime. This is the top-level latency metric for shutdown liveness and shows how long the runtime took to move from shutdown acceptance to completion or forced deadline exit. Level: `detailed`.
    pub drain_total_duration_ns: otap_df_telemetry::instrument::Mmsc,
    /// Count of `DrainIngress` fan-out operations sent to receivers. This confirms that shutdown moved from "requested" to "stop admitting new ingress", which is the first active liveness step of graceful drain. Level: `normal`.
    pub drain_ingress_sent: otap_df_telemetry::instrument::Counter<u64>,
    /// Number of node-control deliveries currently buffered for later retry. A sustained non-zero value indicates the runtime manager is no longer delivering control work immediately and is absorbing backpressure from full node-control inboxes instead of blocking the runtime. Level: `basic`.
    pub pending_sends_buffered: otap_df_telemetry::instrument::Gauge<u64>,
    /// Count of `ReceiverDrained` notifications received from receivers. This shows actual receiver-side drain progress and helps distinguish "no work admitted anymore" from "receivers have really finished draining". Level: `normal`.
    pub receiver_drained_received: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of shutdowns that reached the deadline before natural completion. Any non-zero value here is a direct signal that graceful drain did not complete in time and that liveness relied on the shutdown deadline. Level: `normal`.
    pub shutdown_deadline_forced: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of `RuntimeControlMsg::Shutdown` requests accepted by this runtime. This marks the start of the runtime-managed drain sequence and is the main counter used to correlate later drain-phase transitions. Level: `normal`.
    pub shutdown_received: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of telemetry timer start requests received by the runtime manager. This shows how much of the runtime-control stream is driven by metrics collection scheduling rather than ordinary data-plane coordination. Level: `normal`.
    pub start_telemetry_timer_received: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of `StartTimer` control messages received by the runtime manager. This helps quantify timer creation pressure and can explain growth in `timers.active` or bursts of later `timer_tick.sent` activity. Level: `normal`.
    pub start_timer_received: otap_df_telemetry::instrument::Counter<u64>,
    /// Number of telemetry collection timers currently registered. This distinguishes telemetry scheduling pressure from normal timer traffic and helps explain unexpected `CollectTelemetry` activity. Level: `basic`.
    pub telemetry_timers_active: otap_df_telemetry::instrument::Gauge<u64>,
    /// Count of `TimerTick` control messages sent to nodes. This confirms that due timers are actually being delivered and helps detect starvation where timers accumulate but ticks do not make progress. Level: `normal`.
    pub timer_tick_sent: otap_df_telemetry::instrument::Counter<u64>,
    /// Number of ordinary per-node timers currently registered. This is useful for understanding timer pressure and for spotting leaks where timers are started but not canceled or consumed as expected. Level: `basic`.
    pub timers_active: otap_df_telemetry::instrument::Gauge<u64>,
}

/// Per-field conditional availability in descriptor order.
pub const RUNTIME_CONTROL_METRICS_METRIC_AVAILABILITY: &[Option<&str>] = &[
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
    AVAILABILITY,
];

static RUNTIME_CONTROL_METRICS_DESCRIPTOR: MetricsDescriptor = MetricsDescriptor {
    name: METRIC_SET,
    metrics: &[
        MetricsField {
            name: "cancel_telemetry_timer.received",
            unit: "{message}",
            brief: "Count of telemetry timer cancel requests received by the runtime manager. This is mainly useful for checking that telemetry scheduling remains bounded and does not accumulate stale per-node timers. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "cancel_timer.received",
            unit: "{message}",
            brief: "Count of `CancelTimer` control messages received by the runtime manager. Comparing this with timer starts helps diagnose timer churn, leaks, or mismatches between node scheduling and node cleanup behavior. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "collect_telemetry.sent",
            unit: "{message}",
            brief: "Count of `CollectTelemetry` control messages sent to nodes. This helps confirm telemetry timers are progressing under load and that observability work is not being starved by other runtime-control traffic. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "delay_data.received",
            unit: "{message}",
            brief: "Count of `DelayData` requests received by the runtime manager. This measures how much work is being converted into deferred wakeups, a common cause of control-plane load for retry and batching patterns. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "delay_data.returned_during_drain",
            unit: "{message}",
            brief: "Count of delayed-data items returned immediately because drain is active. This is important for shutdown liveness: it shows the runtime is flushing scheduled work back to origin nodes instead of leaving it stranded behind future wakeup times. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "delayed_data.queued",
            unit: "{message}",
            brief: "Number of delayed-data wakeups currently queued in the runtime manager. This is a key liveness signal for processors that depend on delayed retries or one-shot wakeups: rising backlog means more admitted work is waiting on the shared runtime control path. Level: `basic`.",
            instrument: Instrument::Gauge,
            temporality: None,
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "delayed_data.sent",
            unit: "{message}",
            brief: "Count of delayed-data wakeups sent back to origin nodes. This is the completion-side counterpart to `delay_data.received` and is a direct indicator that deferred work is resuming rather than stalling. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "downstream_shutdown.sent",
            unit: "{message}",
            brief: "Count of downstream `Shutdown` fan-out operations to non-receivers. This marks the handoff from receiver draining to downstream convergence, which is useful when debugging where shutdown latency is spent. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "drain.active",
            unit: "{1}",
            brief: "Whether the runtime is currently in graceful drain mode. This is the top-level liveness phase signal for shutdown analysis: `1` means ingress has been stopped and the runtime is trying to converge to quiescence, `0` means normal operation. Level: `basic`.",
            instrument: Instrument::Gauge,
            temporality: None,
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "drain.pending_receivers",
            unit: "{node}",
            brief: "Number of receivers that still need to report `ReceiverDrained`. This shows whether shutdown is still blocked in the receiver-first drain phase and helps identify a stuck or slow receiver delaying downstream shutdown. Level: `basic`.",
            instrument: Instrument::Gauge,
            temporality: None,
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "drain.receiver_phase_duration_ns",
            unit: "ns",
            brief: "Distribution of time spent waiting for receivers to finish draining. This isolates the receiver-gated phase of shutdown and is useful for determining whether slow graceful shutdown is ingress-bound or further downstream in the pipeline. Level: `detailed`.",
            instrument: Instrument::Mmsc,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::F64,
        },
        MetricsField {
            name: "drain.total_duration_ns",
            unit: "ns",
            brief: "Distribution of total graceful shutdown duration for the runtime. This is the top-level latency metric for shutdown liveness and shows how long the runtime took to move from shutdown acceptance to completion or forced deadline exit. Level: `detailed`.",
            instrument: Instrument::Mmsc,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::F64,
        },
        MetricsField {
            name: "drain_ingress.sent",
            unit: "{message}",
            brief: "Count of `DrainIngress` fan-out operations sent to receivers. This confirms that shutdown moved from \"requested\" to \"stop admitting new ingress\", which is the first active liveness step of graceful drain. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "pending_sends.buffered",
            unit: "{message}",
            brief: "Number of node-control deliveries currently buffered for later retry. A sustained non-zero value indicates the runtime manager is no longer delivering control work immediately and is absorbing backpressure from full node-control inboxes instead of blocking the runtime. Level: `basic`.",
            instrument: Instrument::Gauge,
            temporality: None,
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "receiver_drained.received",
            unit: "{message}",
            brief: "Count of `ReceiverDrained` notifications received from receivers. This shows actual receiver-side drain progress and helps distinguish \"no work admitted anymore\" from \"receivers have really finished draining\". Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "shutdown.deadline_forced",
            unit: "{message}",
            brief: "Count of shutdowns that reached the deadline before natural completion. Any non-zero value here is a direct signal that graceful drain did not complete in time and that liveness relied on the shutdown deadline. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "shutdown.received",
            unit: "{message}",
            brief: "Count of `RuntimeControlMsg::Shutdown` requests accepted by this runtime. This marks the start of the runtime-managed drain sequence and is the main counter used to correlate later drain-phase transitions. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "start_telemetry_timer.received",
            unit: "{message}",
            brief: "Count of telemetry timer start requests received by the runtime manager. This shows how much of the runtime-control stream is driven by metrics collection scheduling rather than ordinary data-plane coordination. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "start_timer.received",
            unit: "{message}",
            brief: "Count of `StartTimer` control messages received by the runtime manager. This helps quantify timer creation pressure and can explain growth in `timers.active` or bursts of later `timer_tick.sent` activity. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "telemetry_timers.active",
            unit: "{timer}",
            brief: "Number of telemetry collection timers currently registered. This distinguishes telemetry scheduling pressure from normal timer traffic and helps explain unexpected `CollectTelemetry` activity. Level: `basic`.",
            instrument: Instrument::Gauge,
            temporality: None,
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "timer_tick.sent",
            unit: "{message}",
            brief: "Count of `TimerTick` control messages sent to nodes. This confirms that due timers are actually being delivered and helps detect starvation where timers accumulate but ticks do not make progress. Level: `normal`.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "timers.active",
            unit: "{timer}",
            brief: "Number of ordinary per-node timers currently registered. This is useful for understanding timer pressure and for spotting leaks where timers are started but not canceled or consumed as expected. Level: `basic`.",
            instrument: Instrument::Gauge,
            temporality: None,
            value_type: MetricValueType::U64,
        },
    ],
};

impl MetricSetHandler for RuntimeControlMetrics {
    #[inline]
    fn descriptor(&self) -> &'static MetricsDescriptor {
        &RUNTIME_CONTROL_METRICS_DESCRIPTOR
    }

    #[inline]
    fn snapshot_values(&self) -> Vec<MetricValue> {
        vec![
            MetricValue::from(self.cancel_telemetry_timer_received.get()),
            MetricValue::from(self.cancel_timer_received.get()),
            MetricValue::from(self.collect_telemetry_sent.get()),
            MetricValue::from(self.delay_data_received.get()),
            MetricValue::from(self.delay_data_returned_during_drain.get()),
            MetricValue::from(self.delayed_data_queued.get()),
            MetricValue::from(self.delayed_data_sent.get()),
            MetricValue::from(self.downstream_shutdown_sent.get()),
            MetricValue::from(self.drain_active.get()),
            MetricValue::from(self.drain_pending_receivers.get()),
            MetricValue::from(self.drain_receiver_phase_duration_ns.get()),
            MetricValue::from(self.drain_total_duration_ns.get()),
            MetricValue::from(self.drain_ingress_sent.get()),
            MetricValue::from(self.pending_sends_buffered.get()),
            MetricValue::from(self.receiver_drained_received.get()),
            MetricValue::from(self.shutdown_deadline_forced.get()),
            MetricValue::from(self.shutdown_received.get()),
            MetricValue::from(self.start_telemetry_timer_received.get()),
            MetricValue::from(self.start_timer_received.get()),
            MetricValue::from(self.telemetry_timers_active.get()),
            MetricValue::from(self.timer_tick_sent.get()),
            MetricValue::from(self.timers_active.get()),
        ]
    }

    #[inline]
    fn clear_values(&mut self) {
        self.cancel_telemetry_timer_received.reset();
        self.cancel_timer_received.reset();
        self.collect_telemetry_sent.reset();
        self.delay_data_received.reset();
        self.delay_data_returned_during_drain.reset();
        self.delayed_data_sent.reset();
        self.downstream_shutdown_sent.reset();
        self.drain_receiver_phase_duration_ns.reset();
        self.drain_total_duration_ns.reset();
        self.drain_ingress_sent.reset();
        self.receiver_drained_received.reset();
        self.shutdown_deadline_forced.reset();
        self.shutdown_received.reset();
        self.start_telemetry_timer_received.reset();
        self.start_timer_received.reset();
        self.timer_tick_sent.reset();
    }

    #[inline]
    fn needs_flush(&self) -> bool {
        if !MetricValue::from(self.cancel_telemetry_timer_received.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.cancel_timer_received.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.collect_telemetry_sent.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.delay_data_received.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.delay_data_returned_during_drain.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.delayed_data_sent.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.downstream_shutdown_sent.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.drain_receiver_phase_duration_ns.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.drain_total_duration_ns.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.drain_ingress_sent.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.receiver_drained_received.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.shutdown_deadline_forced.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.shutdown_received.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.start_telemetry_timer_received.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.start_timer_received.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.timer_tick_sent.get()).is_zero() {
            return true;
        }
        true
    }
}

impl RuntimeControlMetrics {
    /// Registers this metric set against a semantically compatible entity.
    #[must_use]
    pub fn register<E>(registry: &TelemetryRegistryHandle, entity: E) -> MetricSet<Self>
    where
        E: AssociatedEntity,
    {
        registry.register_metric_set(entity)
    }
}
