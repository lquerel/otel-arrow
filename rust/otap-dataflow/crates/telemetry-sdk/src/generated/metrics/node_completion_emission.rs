// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// @generated by OTel Weaver. DO NOT EDIT.

//! Generated client for the `node.completion_emission` metric set.

use crate::entities;
use otap_df_telemetry::descriptor::{Instrument, MetricValueType, MetricsDescriptor, MetricsField};
use otap_df_telemetry::metrics::{MetricSet, MetricSetHandler, MetricValue};
use otap_df_telemetry::registry::TelemetryRegistryHandle;

/// Canonical metric-set name and instrumentation scope.
pub const METRIC_SET: &str = "node.completion_emission";

/// Workspace package containing the current hand-written declaration.
pub const SOURCE_PACKAGE: &str = "otap-df-engine";

/// Conditional availability inherited by the complete metric set.
pub const AVAILABILITY: Option<&str> = None;

/// Marker implemented only by entity types allowed by this metric set's
/// semantic-convention `entity_associations` expression.
pub trait AssociatedEntity: entities::SemanticEntity {}

impl AssociatedEntity for entities::NodeAttributeSet {}

impl AssociatedEntity for entities::NodeWithCustomAttributeSet {}

/// Strongly typed values for the `node.completion_emission` metric set.
#[derive(Debug, Default, Clone)]
#[repr(C, align(64))]
pub struct CompletionEmissionMetrics {
    /// Count of successful `notify_ack` routings into the shared completion lane.
    pub notify_ack_routed: otap_df_telemetry::instrument::Counter<u64>,
    /// Count of successful `notify_nack` routings into the shared completion lane.
    pub notify_nack_routed: otap_df_telemetry::instrument::Counter<u64>,
}

/// Per-field conditional availability in descriptor order.
pub const COMPLETION_EMISSION_METRICS_METRIC_AVAILABILITY: &[Option<&str>] =
    &[AVAILABILITY, AVAILABILITY];

static COMPLETION_EMISSION_METRICS_DESCRIPTOR: MetricsDescriptor = MetricsDescriptor {
    name: METRIC_SET,
    metrics: &[
        MetricsField {
            name: "notify_ack.routed",
            unit: "{message}",
            brief: "Count of successful `notify_ack` routings into the shared completion lane.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "notify_nack.routed",
            unit: "{message}",
            brief: "Count of successful `notify_nack` routings into the shared completion lane.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
    ],
};

impl MetricSetHandler for CompletionEmissionMetrics {
    #[inline]
    fn descriptor(&self) -> &'static MetricsDescriptor {
        &COMPLETION_EMISSION_METRICS_DESCRIPTOR
    }

    #[inline]
    fn snapshot_values(&self) -> Vec<MetricValue> {
        vec![
            MetricValue::from(self.notify_ack_routed.get()),
            MetricValue::from(self.notify_nack_routed.get()),
        ]
    }

    #[inline]
    fn clear_values(&mut self) {
        self.notify_ack_routed.reset();
        self.notify_nack_routed.reset();
    }

    #[inline]
    fn needs_flush(&self) -> bool {
        if !MetricValue::from(self.notify_ack_routed.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.notify_nack_routed.get()).is_zero() {
            return true;
        }
        false
    }
}

impl CompletionEmissionMetrics {
    /// Registers this metric set against a semantically compatible entity.
    #[must_use]
    pub fn register<E>(registry: &TelemetryRegistryHandle, entity: E) -> MetricSet<Self>
    where
        E: AssociatedEntity,
    {
        registry.register_metric_set(entity)
    }
}
