// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// @generated by OTel Weaver. DO NOT EDIT.

//! Generated client for the `processor.wasm_processor.pdata.records` metric set.

use crate::entities;
use otap_df_telemetry::descriptor::{Instrument, MetricValueType, MetricsDescriptor, MetricsField};
use otap_df_telemetry::metrics::{MetricSet, MetricSetHandler, MetricValue};
use otap_df_telemetry::registry::TelemetryRegistryHandle;

/// Canonical metric-set name and instrumentation scope.
pub const METRIC_SET: &str = "processor.wasm_processor.pdata.records";

/// Workspace package containing the current hand-written declaration.
pub const SOURCE_PACKAGE: &str = "otap-df-wasm-host";

/// Conditional availability inherited by the complete metric set.
pub const AVAILABILITY: Option<&str> = Some("feature = \"wasm\"");

/// Marker implemented only by entity types allowed by this metric set's
/// semantic-convention `entity_associations` expression.
pub trait AssociatedEntity: entities::SemanticEntity {}

impl AssociatedEntity for entities::NodeAttributeSet {}

impl AssociatedEntity for entities::NodeWithCustomAttributeSet {}

/// Strongly typed values for the `processor.wasm_processor.pdata.records` metric set.
#[derive(Debug, Default, Clone)]
#[repr(C, align(64))]
pub struct WasmProcessorRecordMetrics {
    /// Records entering the guest (root batch row count).
    pub records_in: otap_df_telemetry::instrument::Counter<u64>,
    /// Records leaving the guest after filtering.
    pub records_out: otap_df_telemetry::instrument::Counter<u64>,
}

/// Per-field conditional availability in descriptor order.
pub const WASM_PROCESSOR_RECORD_METRICS_METRIC_AVAILABILITY: &[Option<&str>] =
    &[AVAILABILITY, AVAILABILITY];

static WASM_PROCESSOR_RECORD_METRICS_DESCRIPTOR: MetricsDescriptor = MetricsDescriptor {
    name: METRIC_SET,
    metrics: &[
        MetricsField {
            name: "records.in",
            unit: "{item}",
            brief: "Records entering the guest (root batch row count).",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
        MetricsField {
            name: "records.out",
            unit: "{item}",
            brief: "Records leaving the guest after filtering.",
            instrument: Instrument::Counter,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::U64,
        },
    ],
};

impl MetricSetHandler for WasmProcessorRecordMetrics {
    #[inline]
    fn descriptor(&self) -> &'static MetricsDescriptor {
        &WASM_PROCESSOR_RECORD_METRICS_DESCRIPTOR
    }

    #[inline]
    fn snapshot_values(&self) -> Vec<MetricValue> {
        vec![
            MetricValue::from(self.records_in.get()),
            MetricValue::from(self.records_out.get()),
        ]
    }

    #[inline]
    fn clear_values(&mut self) {
        self.records_in.reset();
        self.records_out.reset();
    }

    #[inline]
    fn needs_flush(&self) -> bool {
        if !MetricValue::from(self.records_in.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.records_out.get()).is_zero() {
            return true;
        }
        false
    }
}

impl WasmProcessorRecordMetrics {
    /// Registers this metric set against a semantically compatible entity.
    #[must_use]
    pub fn register<E>(registry: &TelemetryRegistryHandle, entity: E) -> MetricSet<Self>
    where
        E: AssociatedEntity,
    {
        registry.register_metric_set(entity)
    }
}
