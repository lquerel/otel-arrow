// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// @generated by OTel Weaver. DO NOT EDIT.

//! Generated client for the `processor.compute` metric set.

use crate::entities;
use otap_df_telemetry::descriptor::{Instrument, MetricValueType, MetricsDescriptor, MetricsField};
use otap_df_telemetry::metrics::{MetricSet, MetricSetHandler, MetricValue};
use otap_df_telemetry::registry::TelemetryRegistryHandle;

/// Canonical metric-set name and instrumentation scope.
pub const METRIC_SET: &str = "processor.compute";

/// Workspace package containing the current hand-written declaration.
pub const SOURCE_PACKAGE: &str = "otap-df-engine";

/// Conditional availability inherited by the complete metric set.
pub const AVAILABILITY: Option<&str> = None;

/// Marker implemented only by entity types allowed by this metric set's
/// semantic-convention `entity_associations` expression.
pub trait AssociatedEntity: entities::SemanticEntity {}

impl AssociatedEntity for entities::NodeAttributeSet {}

impl AssociatedEntity for entities::NodeWithCustomAttributeSet {}

/// Strongly typed values for the `processor.compute` metric set.
#[derive(Debug, Default, Clone)]
#[repr(C, align(64))]
pub struct ComputeDurationMetrics {
    /// Wall-clock duration of failed compute sections, in nanoseconds.
    pub compute_duration_failed: otap_df_telemetry::instrument::Mmsc,
    /// Wall-clock duration of successful compute sections, in nanoseconds.
    pub compute_duration_success: otap_df_telemetry::instrument::Mmsc,
}

/// Per-field conditional availability in descriptor order.
pub const COMPUTE_DURATION_METRICS_METRIC_AVAILABILITY: &[Option<&str>] =
    &[AVAILABILITY, AVAILABILITY];

static COMPUTE_DURATION_METRICS_DESCRIPTOR: MetricsDescriptor = MetricsDescriptor {
    name: METRIC_SET,
    metrics: &[
        MetricsField {
            name: "compute.failed.duration",
            unit: "ns",
            brief: "Wall-clock duration of failed compute sections, in nanoseconds.",
            instrument: Instrument::Mmsc,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::F64,
        },
        MetricsField {
            name: "compute.success.duration",
            unit: "ns",
            brief: "Wall-clock duration of successful compute sections, in nanoseconds.",
            instrument: Instrument::Mmsc,
            temporality: Some(otap_df_telemetry::descriptor::Temporality::Delta),
            value_type: MetricValueType::F64,
        },
    ],
};

impl MetricSetHandler for ComputeDurationMetrics {
    #[inline]
    fn descriptor(&self) -> &'static MetricsDescriptor {
        &COMPUTE_DURATION_METRICS_DESCRIPTOR
    }

    #[inline]
    fn snapshot_values(&self) -> Vec<MetricValue> {
        vec![
            MetricValue::from(self.compute_duration_failed.get()),
            MetricValue::from(self.compute_duration_success.get()),
        ]
    }

    #[inline]
    fn clear_values(&mut self) {
        self.compute_duration_failed.reset();
        self.compute_duration_success.reset();
    }

    #[inline]
    fn needs_flush(&self) -> bool {
        if !MetricValue::from(self.compute_duration_failed.get()).is_zero() {
            return true;
        }
        if !MetricValue::from(self.compute_duration_success.get()).is_zero() {
            return true;
        }
        false
    }
}

impl ComputeDurationMetrics {
    /// Registers this metric set against a semantically compatible entity.
    #[must_use]
    pub fn register<E>(registry: &TelemetryRegistryHandle, entity: E) -> MetricSet<Self>
    where
        E: AssociatedEntity,
    {
        registry.register_metric_set(entity)
    }
}
